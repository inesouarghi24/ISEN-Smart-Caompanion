<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ISENSmartCompanion"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.ai.API_KEY"
            android:value="@string/gemini_api_key" />



        <!-- 📌 Splash Screen  -->
        <activity
            android:name=".screens.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ISENSmartCompanion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 📌 Écran des détails d'un event -->
        <activity
            android:name=".screens.EventDetailActivity"
            android:exported="false"
            android:label="@string/title_activity_event_detail"
            android:theme="@style/Theme.ISENSmartCompanion" />

        <!-- 📌 Écran principal de l'application après mon splash screen hello kitty -->
        <activity
            android:name=".screens.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ISENSmartCompanion">
        </activity>

    </application>

</manifest>
